spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL #bdd memory
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true

customer:
  service:
    url: http://localhost:8081

logging:
  level:
    com.banking: DEBUG
